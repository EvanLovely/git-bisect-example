{
  "name": "git-bisect-example",
  "version": "1.0.0",
  "description": "An example repo to demo how to use `git bisect`.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files index.html style.css"
  },
  "keywords": ["example", "git"],
  "author": "Evan Lovely",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.13.0"
  }
}
